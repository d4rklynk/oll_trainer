// Add an event listener for the DOMContentLoaded event
window.addEventListener('DOMContentLoaded', function() {
  // Define a function to change the mode
  function changeMode() {
    // Use strict mode
    'use strict';
    // Change the mode
    console.log('Mode changed');
  }

  // Call the function to change the mode
  changeMode();
});
